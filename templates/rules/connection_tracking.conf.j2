{{ ansible_managed | comment }}

domain $FERM_DOMAINS table filter chain (INPUT FORWARD) {
    mod state state INVALID DROP;
    mod state state (ESTABLISHED RELATED) ACCEPT;
}

domain $FERM_DOMAINS table filter chain (OUTPUT) {
    mod state state (ESTABLISHED RELATED) ACCEPT;
}
